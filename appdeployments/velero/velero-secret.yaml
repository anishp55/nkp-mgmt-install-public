kubectl create secret generic nutanix-object-credentials -n test01-rdq46 --from-literal=${SECRET_KEY_NAME}="[${AWS_PROFILE}]


apiVersion: v1
data:
  ${SECRET_KEY_NAME}: ${PROFILEDATA}
kind: Secret
metadata:
  labels:
    kommanderType: connectorSecret
  name: ${NUTANIX_OBJECTS_SECRET}
  namespace: kommander
type: Opaque


[${AWS_PROFILE}]
aws_access_key_id=<key>
aws_secret_access_key=<secrey>